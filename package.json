{
    "name": "unified-backend-service",
    "version": "1.0.0",
    "description": "Gateway for Kanban, MindLit, RapidCare, and Webhuiyans backends",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "install-all": "npm install && cd apps/kanban && npm install && cd ../mindlit && npm install && cd ../rapidcare && npm install && cd ../webhuiyans && npm install",
        "postinstall": "echo 'Run npm run install-all to install sub-dependencies'"
    },
    "dependencies": {
        "express": "^4.18.2",
        "http-proxy-middleware": "^2.0.6",
        "dotenv": "^16.3.1",
        "cors": "^2.8.5"
    },
    "engines": {
        "node": ">=18"
    }
}